<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true"></application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />

	<!-- Scanning barcodes. Requested at the moment the scan screen opens, never at startup. -->
	<uses-permission android:name="android.permission.CAMERA" />

	<!-- The app works without a camera: manual barcode entry is always available. Marking the
	     feature optional keeps it installable on devices that have none. -->
	<uses-feature android:name="android.hardware.camera" android:required="false" />
	<uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
</manifest>